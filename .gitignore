node_modules

# ── Environment / secrets ────────────────────────────────────────────────────
# These files contain API keys, private keys, and tokens — never commit them.
.env
.env.local
.env.production
.env.staging
*.p8
*.pem
*.key

# build output
frontend/dist
backend/dist
dist

.claude/
.DS_Store
**/.DS_Store
CONTEXT.md
